# Copy to .env and adjust as needed for your local setup.
# Placeholder value below matches the docker-compose Postgres service:
# db "todolist", user "todolist", password "todolist", port 5432.
DATABASE_URL="postgresql://todolist:todolist@localhost:5432/todolist"
